# ============================================================
# Supabase — configuração da ficha de anamnese e do painel
#
# NÃO é obrigatório criar este arquivo: a URL e a chave pública (anon)
# ficam embutidas no código (src/lib/supabase.ts). Este exemplo serve
# apenas se você quiser sobrescrever os valores por ambiente.
# ============================================================

# Público (protegido pelo RLS — pode ir para o navegador).
# Project Settings > API Keys
VITE_SUPABASE_URL=https://jjkmgkorqzbroebhksca.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...anon...
